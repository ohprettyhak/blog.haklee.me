import * as React from 'react';

import Layout from '../components/layout';

const TravelPage: React.FC = React.memo(() => {
  return (
    <Layout>
      <></>
    </Layout>
  );
});

export default TravelPage;
